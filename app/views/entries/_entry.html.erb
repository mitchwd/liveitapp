<div class="article">
  
  <h3><%= @entry.title %></h3>
  
  <p>
    <%= @entry.content %><br /><br />
    <em><%= @entry.verse_content %></em> (<%= link_to @entry.verse_reference + " " + @entry.verse_translation, "http://www.biblegateway.com/passage/?version=#{@entry.verse_translation}&search=#{@entry.verse_reference}", target: "_blank" %>)
  </p>
  
  <%= button_to "LiveIt", liveit_path(@entry) %><br />
  Share it: ...<br />
  <br />
  <div class="social">
    <%= pluralize(@entry.users.count, "person") %> lived this out.<br />
    <% @entry_users.each do |user| %>
      <div class="user user_<%= user.name %>">
        [photo] <%= user.name %>
      </div>
    <% end %>
  </div>
</div>